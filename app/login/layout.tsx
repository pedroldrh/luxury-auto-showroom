export const metadata = {
  title: "Private Client Portal | Prestige Motors",
  description:
    "Access your private collection, service history, and exclusive events.",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
