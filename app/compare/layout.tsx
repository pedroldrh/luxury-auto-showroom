export const metadata = {
  title: "Compare Vehicles | Prestige Motors",
  description:
    "Compare specifications side-by-side for the world's finest automobiles.",
};

export default function CompareLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
