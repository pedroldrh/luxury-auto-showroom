export const metadata = {
  title: "Concierge | Prestige Motors",
  description:
    "Experience our private client services — vehicle sourcing, worldwide shipping, custom builds, and investment consulting.",
};

export default function ConciergeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
